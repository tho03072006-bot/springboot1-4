<%@ page contentType="text/html;charset=UTF-8" pageEncoding="UTF-8" %>
<%@ taglib prefix="c" uri="jakarta.tags.core" %>
<%@ taglib prefix="fn" uri="jakarta.tags.functions" %>
<%@ taglib prefix="form" uri="http://www.springframework.org/tags/form" %>
<html lang="vi"><head><meta charset="UTF-8"><title>Người dùng</title></head><body><div class="page-head"><div><p class="eyebrow">Quản lý dữ liệu</p><h1>Người dùng</h1><p>Tìm kiếm theo tên đăng nhập, họ tên, email hoặc số điện thoại.</p></div><a class="btn btn-primary" href="${pageContext.request.contextPath}/admin/users/new">+ Thêm mới</a></div>
<section class="panel"><form method="get" action="${pageContext.request.contextPath}/admin/users" class="searchbar"><div class="search-input"><label for="q">Từ khóa tìm kiếm</label><input id="q" name="q" class="form-control" placeholder="Nhập từ khóa…" maxlength="100" value="${fn:escapeXml(q)}"></div><div><label for="size">Mỗi trang</label><select id="size" name="size" class="form-select"><c:forEach items="${[5,10,20]}" var="s"><option value="${s}" ${s == result.size ? 'selected' : ''}>${s} dòng</option></c:forEach></select></div><button class="btn btn-primary" style="padding:10px 18px">Tìm kiếm</button><a class="btn btn-light" style="padding:10px 18px" href="${pageContext.request.contextPath}/admin/users">Đặt lại</a></form>
<div class="table-responsive"><table class="table"><thead><tr><th>ID</th><th>Người dùng</th><th>Email</th><th>Quyền</th><th>Trạng thái</th><th class="text-end">Thao tác</th></tr></thead><tbody><c:forEach items="${result.content}" var="item"><tr><td class="text-muted">#${item.id}</td><td><a href="${pageContext.request.contextPath}/admin/users/${item.id}"><strong><c:out value="${item.fullname}"/></strong></a><div class="small text-muted"><c:out value="${item.username}"/></div></td><td><c:out value="${item.email}"/></td><td><span class="badge text-bg-light"><c:out value="${item.role}"/></span></td><td><span class="badge ${item.enabled ? 'text-bg-success' : 'text-bg-secondary'}">${item.enabled ? 'Hoạt động' : 'Đã khóa'}</span></td><td class="actions"><a class="btn btn-sm btn-light" href="${pageContext.request.contextPath}/admin/users/${item.id}">Xem</a> <a class="btn btn-sm btn-light" href="${pageContext.request.contextPath}/admin/users/${item.id}/edit">Sửa</a> <c:if test="${item.username != pageContext.request.userPrincipal.name}"><form method="post" action="${pageContext.request.contextPath}/admin/users/${item.id}/delete" data-confirm="Bạn có chắc muốn xóa bản ghi này?"><input type="hidden" name="${_csrf.parameterName}" value="${_csrf.token}"><button class="btn btn-sm btn-outline-danger">Xóa</button></form></c:if></td></tr></c:forEach></tbody></table></div><c:if test="${empty result.content}"><div class="empty"><h2>Chưa có kết quả</h2><p>Thử từ khóa khác hoặc thêm bản ghi mới.</p></div></c:if><%@ include file="../pagination.jsp" %></section></body></html>
